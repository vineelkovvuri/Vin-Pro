# OS X Post Install Setup

### Vim pathogen plugin
mkdir -p ~/.vim/autoload ~/.vim/bundle && \
		  curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim

go to ~/.vim/bundle

	## Install ctrlp.vim plugin
	run git clone https://github.com/kien/ctrlp.vim.git

