#include<stdio.h>
float sin(float,float);
main()
{
float x,n;
printf("Enter x,n");
scanf("%f%f",&x,&n);
printf("the value of sin(%f) is %f",x,sin(x,n));
}
float sin(float x,float n)
{int i=1;
if(i>n)break;
if(i=1)return(x);


