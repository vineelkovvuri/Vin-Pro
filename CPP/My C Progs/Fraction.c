#include<stdio.h>
#include<math.h>
main()
{
int i,n,d,a,b;
printf("\nEnter the number");
scanf("%d",&n);
printf("\nHow many decimal places are there in the given number:");
scanf("%d",&d);
a=n*pow(10,d);
b=pow(10,d);






getch();
return 0;
}